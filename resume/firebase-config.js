// Firebase client config (safe to be public).
// Fill it from Firebase Console → Project settings → Your apps (Web).
//
// IMPORTANT:
// - This is NOT a secret. Security is enforced by Firestore Rules + Auth.
// - Do NOT put any AI keys here.

export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};

