// Public runtime config (safe to commit).
// For GitHub Pages this file is the easiest place to change URLs without a build step.

window.__RESUME_CONFIG__ = {
  // Cloudflare Worker URL (no trailing slash). Example: "https://my-resume-worker.myname.workers.dev"
  aiApiBaseUrl: "",
};

